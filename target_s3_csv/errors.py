VALIDATION_ERROR = ("Data validation failed and cannot load to destination. \n"
"'multipleOf' validations that allows long precisions are not supported"
" (i.e. with 15 digits or more). Try removing 'multipleOf' methods from JSON schema.")

SCHEMA_ERROR = ("A record for stream {}"
                "was encountered before a corresponding schema")